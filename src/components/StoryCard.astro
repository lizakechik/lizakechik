---
const { story } = Astro.props;
---

<article class="story-card">

<a href={`/stories/${story.id}`}>

<h4>
{story.data.title}
</h4>

<p>
{story.data.description}
</p>

<span>
{story.data.company} · {story.data.role}
</span>

</a>

</article>
